{
  "name": "resourcify-root",
  "version": "1.0.0",
  "description": "Root package to run frontend and backend together",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
